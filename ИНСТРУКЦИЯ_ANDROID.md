# 📱 Инструкция по созданию Android приложения

## Что уже готово:
✅ Настроен Capacitor для Android
✅ Приложение работает офлайн
✅ Иконка и манифест настроены
✅ Конфигурация для сборки APK

## Способ 1: Автоматическая сборка (рекомендуется)

### Через PWA Builder (самый простой):
1. Перейдите на https://www.pwabuilder.com/
2. Введите URL вашего сайта
3. Нажмите "Start" → "Package for Stores"
4. Выберите "Android" → "Generate"
5. Скачайте готовый APK файл

**Преимущества:**
- Не требует установки Android Studio
- APK готов за 2 минуты
- Работает на всех Android 5.0+

## Способ 2: Через Capacitor (для разработчиков)

### Требования:
- Node.js установлен
- Android Studio установлен
- JDK 11+ установлен

### Шаги:

1. **Соберите веб-версию:**
   ```bash
   npm run build
   ```

2. **Инициализируйте Capacitor:**
   ```bash
   npx cap init "Книга Рецептов" "dev.poehali.recipes" --web-dir=dist
   ```

3. **Добавьте Android:**
   ```bash
   npx cap add android
   ```

4. **Синхронизируйте:**
   ```bash
   npx cap sync android
   ```

5. **Откройте в Android Studio:**
   ```bash
   npx cap open android
   ```

6. **Соберите APK в Android Studio:**
   - Build → Build Bundle(s) / APK(s) → Build APK(s)
   - APK будет в: `android/app/build/outputs/apk/debug/app-debug.apk`

## Способ 3: Через Capacitor CLI (быстрая сборка)

Если Android Studio настроен:
```bash
./build-android.sh
cd android
./gradlew assembleDebug
```

APK файл: `android/app/build/outputs/apk/debug/app-debug.apk`

## Как распространять APK:

### Вариант A: Прямая загрузка
1. Загрузите APK на ваш сервер/хостинг
2. Дайте пользователям ссылку на скачивание
3. При установке нужно разрешить "Неизвестные источники"

### Вариант B: Через файлообменники
- Google Drive
- Dropbox
- Telegram канал
- Ваш сайт

### Вариант C: Публикация в магазины
- **Google Play Console** ($25 одноразово)
- **Amazon Appstore** (бесплатно)
- **Huawei AppGallery** (бесплатно)
- **Samsung Galaxy Store** (бесплатно)

## Подпись APK для публикации:

Для Google Play нужен подписанный APK:

```bash
cd android
./gradlew assembleRelease
```

Создайте keystore:
```bash
keytool -genkey -v -keystore recipes-key.keystore -alias recipes -keyalg RSA -keysize 2048 -validity 10000
```

Настройте `android/app/build.gradle` с вашим keystore.

## Совместимость:
- Минимальная версия: Android 5.0 (API 21)
- Целевая версия: Android 14 (API 34)
- Размер APK: ~5-10 МБ

## Тестирование:
1. Установите APK на реальное устройство
2. Проверьте работу офлайн (включите режим полета)
3. Проверьте все разделы: Главная, Рецепты, Советы
4. Убедитесь, что изображения загружаются

## Поддержка:
- Документация Capacitor: https://capacitorjs.com/docs/android
- PWA Builder: https://docs.pwabuilder.com/
